# EduPilot 面试准备（20 问）

> 每个问题给「回答要点」，都能结合 EduPilot 的真实实现。面试时用自己的话复述，别背。

---

## 一、项目介绍类

**1. 介绍一下你的项目**
答：EduPilot 是个 AI 教育实训平台，我 12 周从 0 到 1 搭的。五大模块：RAG 知识库（BGE-zh + Chroma）、教学辅导 Agent（ReAct + 四工具）、智能答疑（FastAPI + 多轮记忆）、多 Agent 编排（LangGraph）、评测体系（数据集 + LLM-as-judge + A/B）。每块都跑通并有实测数据。

**2. 为什么做这个项目？**
答：目标岗位是 AI 研发实习，JD 要求 RAG/Agent/评测/多智能体，我就对照 JD 逐条实现，等于把岗位要求变成了项目清单。

**3. 最大的技术难点？**
答：两个。① chromadb 1.x 的 embedding 协议变更（要同时实现 4 个方法），踩了三次才跑通；② W9 评测发现"加了 RAG 反而更差"，深挖出检索召回不完整 + 测试集太基础两个根因。

**4. 最有成就感的一件事？**
答：用 A/B 实验**诚实发现** RAG 在基础题上会拖后腿，而不是自欺欺人说"RAG 一定更好"。这让我真正理解了评测的意义。

---

## 二、RAG 类

**5. RAG 是什么？为什么减少幻觉？**
答：Retrieval-Augmented Generation，检索增强生成。先把知识切片向量化入库，提问时先检索相关片段，拼进 prompt 让模型"照着资料答"。因为答案被限定在真实资料里，资料没有就让模型说没有，所以减少幻觉、答案可追溯。

**6. 为什么换中文 embedding？**
答：默认的 MiniLM 是英文模型，中文语义距离失真。我实测问"什么是闭包"，正确定义被排到第 5。换成 BGE-zh 后 top1 命中率 2/4→4/4。

**7. bi-encoder vs cross-encoder？**
答：bi-encoder 把 query 和 doc 各自编码后算余弦，快但看不到交互、排序是近似；cross-encoder 把 (query, doc) 拼一起逐对打分，准但慢。所以工业上是两段式：bi-encoder 召回 + cross-encoder 精排。

**8. 为什么 RAG 有时反而更差？**
答：这是我 A/B 实验发现的。RAG 净收益 = 检索增益 − 模型自身知识。当模型很强、知识库小、检索召回不完整时，RAG 反而带偏模型。根因：切片太碎导致正确答案 chunk 排到 top3 外 + "只依据教材"约束太强逼模型硬编。

---

## 三、Agent 类

**9. Agent 的本质？**
答：大模型 + 工具 + 循环。大模型负责决策（调什么工具、传什么参），工具负责执行，循环把两者串起来反复交替。

**10. ReAct 是什么？**
答：Reasoning + Acting 交替。模型先"推理"出该调什么工具（返回 tool_calls），代码"行动"执行工具，结果回传，模型再推理——直到不再调工具给出最终答案。

**11. Function Calling 五步？**
答：① 问 → ② 模型决定调哪个工具传什么参（tool_calls）→ ③ 代码执行工具 → ④ 结果作为 tool 消息回传 → ⑤ 模型生成最终答案（可循环）。

**12. 多 Agent 和单 Agent 区别？**
答：单 Agent 是一个 LLM 反复调工具；多 Agent 是多个不同人设的 LLM 调用按图编排、各司其职。本质是"分工协作" vs "单打独斗"，多 Agent 适合分阶段流水线（备课→批改）。

---

## 四、评测类

**13. 怎么评测 LLM 应用？**
答：固定测试集 + 指标 + A/B 实验。我搭了 8 题测试集，用两个互补指标：关键词命中（客观）和 LLM-as-judge（更准），再控制变量对比两版。

**14. LLM-as-judge 是什么？**
答：用强模型当评委自动打分（我让 DeepSeek 打 0-10 分 + 判断是否编造）。成本低、可规模化，是当前业界标准做法，配少量人工校准。

**15. A/B 实验为什么要控制变量？**
答：保证结果差异只能归因于改变的那一个变量。我的 A/B 里，同模型、同问题、同 temperature，唯一差别是"有没有教材上下文"，所以差异只能归因于 RAG。

---

## 五、工程类

**16. 多轮对话怎么实现？**
答：保存完整 messages 历史（含 tool 消息），每轮"历史 + 新问题"一起发。session_id 隔离不同用户，同一 session 连续调用即多轮。

**17. LangGraph 的 State/Node/Edge？**
答：State 是贯穿流程的共享状态（显式、可追踪）；Node 是一个处理步骤（一个 Agent）；Edge 是节点间连接决定顺序。编译后 invoke 执行。

**18. 为什么用 FastAPI？async 的好处？**
答：类型注解自动生成 OpenAPI 文档 + 请求校验；async 让 LLM 这种 IO 等待不阻塞事件循环，并发高（我实测 4 并发 1180ms ≈ 单次）。

**19. 流式输出怎么实现？**
答：SSE（Server-Sent Events），服务端每生成一个 token 就 `data: ...\n\n` 吐一块，前端边收边渲染。首字 747ms 到 vs 同步 2636ms。

---

## 六、开放类

**20. 如果重新做，你会怎么改进？**
答：① 评测集换成"模型不知道的领域知识"，更能体现 RAG 价值；② 语义切片 + rerank 提升检索；③ 放宽"只依据教材"约束；④ 历史加 token 截断/摘要压缩；⑤ 加流式 token 级输出。
